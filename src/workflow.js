const axiosDefault = require("axios");

// ? Define axios instance
// const axios = axiosDefault.create({
//   baseURL: URL,
// });

exports.main = async (event, callback) => {
  // ? Export inputFields
  // const { INPUT_FIELD } = event.inputFields;

  // ? Get ENV Variables
  // const { ENV_VARIABLE } = process.env;

  try {
    // ? Have Success Callback function
    // callback({
    //   outputFields: {
    //     error: false,
    //   },
    // });
  } catch (err) {
    // ? Have Error Callback function
    // callback({
    //   outputFields: {
    //     error: true,
    //   },
    // });
  }
};
